
import React from 'react';

// API Key management is handled via environment variables as per requirements.
// The application must not ask the user for it under any circumstances.
const ApiKeyOverlay: React.FC = () => {
  return null;
};

export default ApiKeyOverlay;
